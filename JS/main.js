
/*let sinSeleccion = true;

while (sinSeleccion) {
    let opcion = mostrarPeliculas()
    if (opcion >= 1 && opcion <= 10){
        let peliculaElegida = peliculas.find(pelicula => pelicula.id === opcion);
        let cantidadEntradas = entradas ()
        peliculaElegida.entradas = cantidadEntradas;
        peliculaElegida.costoEntradas = cantidadEntradas * precioEntradas;
        compra.push(peliculaElegida);  
        console.log (opcion);
        console.log (peliculaElegida.titulo);
        sinSeleccion = false;
    }else {     
        alert ("Ingrese una opcion valida")
    }
}

let comprarCombo = true;

while (comprarCombo) {
    let seleccion = mostrarCombos ()
    if (seleccion >=1 && seleccion <= 4){
        let comboSeleccionado = combos.find (combo => combo.id === seleccion);
            let eleccion = mostrarTamaños ()
            if (eleccion >=1 && eleccion <= 3){
                let tamañoSeleccionado = tamaños.find (tamaño => tamaño.id === eleccion)
                comboSeleccionado.tamaño = tamañoSeleccionado.especificacion
                comboSeleccionado.factorModificador = tamañoSeleccionado.factorModificador
            }else {
                alert ("Ingrese una opcion valida")
            }
        compraCombos.push(comboSeleccionado);
    }else if (seleccion == "0") {
        comprarCombo = false;
    } else {     
        alert ("Ingrese una opcion valida")
    }
}


resumenCompra ();*/





